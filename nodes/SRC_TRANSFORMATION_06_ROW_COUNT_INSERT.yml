---
name: SRC_TRANSFORMATION_06_ROW_COUNT_INSERT
type: transformation
transformation_type: ROW_COUNT
execution_order: 6
description: Counts new insert rows where lookup had no match

source:
  type: upstream_transformation
  name: SRC_TRANSFORMATION_04_CONDITIONAL_SPLIT
  output_branch: INSERT_ROWS
  description: Rows with no match in lookup - new records

transformation_logic:
  operation: COUNT_ROWS
  count_variable: User::InsertCount
  filter_condition: EDWBeanVersion IS NULL
  description: Count rows that will be inserted as new records

output:
  variable_name: User::InsertCount
  variable_type: Integer
  description: Total count of new insert rows
  row_disposition: INSERT_TO_DIMENSION

upstream_dependencies:
  - SRC_TRANSFORMATION_04_CONDITIONAL_SPLIT

metadata:
  package_name: EDW_BC_Load_DimBillingAccount
  component_type: Row Count
  execution_sequence: 6
  purpose: Track count of new records for monitoring and validation