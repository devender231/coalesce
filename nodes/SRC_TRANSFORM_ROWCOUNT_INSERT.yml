fileVersion: 1.0
name: SRC_TRANSFORM_ROWCOUNT_INSERT
description: Counts new insert rows (lookup no match)
type: RowCount
config:
  transformation_type: ROW_COUNT
  logic:
    description: Counts new insert rows where lookup found no match
    variable: User::InsertCount
    operation: COUNT_ROWS
  input:
    source: SRC_TRANSFORM_CONDITIONAL_SPLIT
    output_name: InsertOutput
    description: Rows from conditional split with no match in target table
  output:
    variable_name: InsertCount
    variable_type: Integer
    scope: User

execution_order: 7
package: EDW_BC_Load_DimBillingAccount
dependencies:
  - SRC_TRANSFORM_CONDITIONAL_SPLIT