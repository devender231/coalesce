name: SRC_TR_ROWCOUNT_UPDATE
type: transformation
transformation_type: ROW_COUNT
execution_sequence: 7
description: Counts update rows (BeanVersion different)

input:
  source: SRC_TR_CONDITIONALSPLIT_BEANVERSION
  output_path: UPDATE_OUTPUT
  description: Takes update output from conditional split

logic:
  operation: COUNT_ROWS
  variable: User::UpdateCount
  description: Stores count of update rows in variable User::UpdateCount

output:
  name: ROWCOUNT_UPDATE_OUTPUT
  columns: ALL
  metadata:
    row_count_variable: User::UpdateCount
    description: Existing rows to be updated in DimBillingAccount
    target_operation: UPDATE