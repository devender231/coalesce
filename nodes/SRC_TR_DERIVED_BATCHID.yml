# Transformation: Derived Column - BatchID
# Package: EDW_BC_Load_DimBillingAccount
# Description: Adds or overwrites BatchID column with current batch identifier

name: SRC_TR_DERIVED_BATCHID
type: transformation
transformation_type: DERIVED_COLUMN
version: 1.0
execution_order: 2

input:
  source: SRC_TR_ROW_COUNT_SOURCE
  description: "Dataset after source row count"

transformation_logic:
  operation: ADD_DERIVED_COLUMN
  description: "Adds BatchID column from SSIS variable"
  columns:
    - column_name: BatchID
      expression: "@[User::BatchID]"
      data_type: INTEGER
      nullable: false
      description: "Batch identifier for tracking data load batches"
      operation_type: "ADD_OR_REPLACE"

output:
  name: BATCHID_ADDED
  description: "Dataset with BatchID column added"
  includes_all_input_columns: true
  new_columns:
    - BatchID

metadata:
  created_by: "Data Engineering Automation Agent"
  source_package: "EDW_BC_Load_DimBillingAccount"
  component_type: "Derived Column"
  purpose: "Add batch tracking identifier"
  execution_sequence: 2
  variable_reference: "User::BatchID"
