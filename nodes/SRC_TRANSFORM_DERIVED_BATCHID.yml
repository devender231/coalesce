fileVersion: 1.0
name: SRC_TRANSFORM_DERIVED_BATCHID
description: Adds or overwrites BatchID column
type: DerivedColumn
config:
  transformation_type: DERIVED
  logic:
    description: Adds or overwrites BatchID column
    derived_columns:
      - column_name: BatchID
        expression: '@[User::BatchID]'
        data_type: INTEGER
        operation: ADD_OR_REPLACE
  input:
    source: SRC_TRANSFORM_ROWCOUNT_SOURCE
    description: Output from source row count
  output:
    columns:
      - BatchID
    description: Dataset with BatchID column added

execution_order: 3
package: EDW_BC_Load_DimBillingAccount
dependencies:
  - SRC_TRANSFORM_ROWCOUNT_SOURCE