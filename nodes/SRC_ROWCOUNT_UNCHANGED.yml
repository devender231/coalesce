fileVersion: 1
id: SRC_ROWCOUNT_UNCHANGED
name: SRC_ROWCOUNT_UNCHANGED
operation:
  config:
    insertStrategy: INSERT
    selectDistinct: false
  database: ""
  deployEnabled: true
  description: "ROW_COUNT Transformation - Counts unchanged rows where BeanVersion matches EDW_BEANVERSION and stores in Variable User::UnChangeCount"
  isMultisource: false
  locationName: SRC
  materializationType: view
  metadata:
    appliedNodeTests: []
    columns:
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: 1
          stepCounter: 1
        config: {}
        dataType: NUMBER
        description: Row count of unchanged records
        name: UNCHANGED_ROW_COUNT
        nullable: false
        sourceColumnReferences:
          - columnReferences: []
            transform: COUNT(*)
    cteString: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          SPLIT: SPLIT
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: SRC
            nodeName: SRC_CONDITIONALSPLIT_BEANVERSION
        join:
          joinCondition: |
            FROM {{ ref('SRC', 'SRC_CONDITIONALSPLIT_BEANVERSION') }} SPLIT
            WHERE SPLIT.SPLIT_CATEGORY = 'UNCHANGED'
        name: SRC_CONDITIONALSPLIT_BEANVERSION
        noLinkRefs: []
  name: SRC_ROWCOUNT_UNCHANGED
  overrideSQL: false
  schema: ""
  sqlType: DT113:::156
  type: sql
  version: 1
type: Node
