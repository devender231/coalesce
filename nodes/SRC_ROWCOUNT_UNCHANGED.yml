name: SRC_ROWCOUNT_UNCHANGED
type: transformation
config:
  transformation_type: row_count
  description: "Counts unchanged rows (BeanVersion matches)"
  execution_order: 5
dependencies:
  - SRC_CONDITIONALSPLIT_BEANVERSION
logic:
  operation: count_rows
  variable_name: "User::UnChangeCount"
  input_branch: UnchangedRows
  description: "Captures count of rows where BeanVersion matches EDWBeanVersion"
output:
  row_count_variable: "User::UnChangeCount"
  pass_through_data: false
  description: "Counts rows and terminates this branch (no further processing needed)"