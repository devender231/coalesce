# Row Count Transformation - Unchanged Rows
# Package: EDW_BC_Load_DimBillingAccount
# Description: Counts unchanged rows (BeanVersion matches)

name: SRC_ROWCOUNT_UNCHANGED
type: TRANSFORMATION
transformation_type: ROW_COUNT
version: 1.0

source:
  name: UNCHANGED_OUTPUT
  description: Rows with matching BeanVersion from conditional split
  upstream_dependency: SRC_CONDITIONALSPLIT_SCD

transformation_logic:
  operation: COUNT_ROWS
  description: Counts rows where BeanVersion matches EDWBeanVersion - no updates needed
  variable_assignment:
    variable_name: User::UnChangeCount
    variable_type: INTEGER
    scope: PACKAGE
    description: Stores count of unchanged records

output:
  name: UNCHANGED_COUNTED
  description: Pass-through dataset with unchanged count captured
  row_count_variable: User::UnChangeCount
  disposition: NO_LOAD

execution_order: 5

metadata:
  created_from: SSIS_Documentation/EDW_BC_Load_DimBillingAccount.json
  transformation_sequence: 5
  purpose: Audit and monitoring - track unchanged row count
  load_action: SKIP