name: SRC_TR_ROW_COUNT_SOURCE
type: ROW_COUNT
execution_order: 1
description: Counts source rows from OLE_SRC - GuideWire

input:
  source: SRC_JOIN_DIMBILLINGACCOUNT
  description: Joined data from all source tables

transformation_logic:
  operation: COUNT_ROWS
  variable_name: User::SourceCount
  description: Stores the count of source rows before any transformations

output:
  name: SOURCE_ROW_COUNT
  variable: User::SourceCount
  pass_through: true
  description: Passes all rows through while counting them