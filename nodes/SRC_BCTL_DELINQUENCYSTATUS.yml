name: SRC_BCTL_DELINQUENCYSTATUS
type: source
config:
  location: SRC ("BC"."dbo")
  columns:
    - name: ID
      dataType: INTEGER
      nullable: true
    - name: NAME
      dataType: STRING
      nullable: true