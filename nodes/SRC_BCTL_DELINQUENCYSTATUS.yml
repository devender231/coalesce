name: SRC_BCTL_DELINQUENCYSTATUS
type: source
config:
  source_name: BCTL_DELINQUENCYSTATUS
  source_type: table
  database: ""
  schema: ""
  location: SRC ("."."")
columns:
  - name: DELINQUENCYSTATUSKEY
    datatype: NUMBER(38,0)
    description: Primary key for delinquency status
  - name: DELINQUENCYSTATUSCODE
    datatype: VARCHAR2(50)
    description: Delinquency status code
  - name: DELINQUENCYSTATUSNAME
    datatype: VARCHAR2(200)
    description: Delinquency status name
  - name: DELINQUENCYSTATUSDESCRIPTION
    datatype: VARCHAR2(500)
    description: Delinquency status description