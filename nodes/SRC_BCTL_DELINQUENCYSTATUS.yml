fileVersion: 1.0
name: SRC_BCTL_DELINQUENCYSTATUS
type: source
description: Source table BCTL_DELINQUENCYSTATUS from GuideWire

source:
  location:
    database: "GUIDEWIRE"
    schema: "BCTL"
    table: "BCTL_DELINQUENCYSTATUS"

columns:
  - name: ID
    dataType: INTEGER
    nullable: true
  - name: NAME
    dataType: STRING
    nullable: true