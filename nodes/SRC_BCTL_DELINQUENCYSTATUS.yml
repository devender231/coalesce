source:
  name: SRC_BCTL_DELINQUENCYSTATUS
  location: SRC("BC"."BCTL_DELINQUENCYSTATUS")
  columns:
    - name: ID
      data_type: INTEGER
      nullable: true
    - name: NAME
      data_type: STRING
      nullable: true
