name: SRC_BCTL_DELINQUENCYSTATUS
type: source
config:
  source_type: table
  source_location:
    database: GUIDEWIRE
    schema: BCTL
    table: BCTL_DELINQUENCYSTATUS
columns:
  - name: ID
    data_type: INTEGER
    nullable: true
  - name: NAME
    data_type: STRING
    nullable: true