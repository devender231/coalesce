name: SRC_BCTL_DELINQUENCYSTATUS
type: source
config:
  location: BC.DBO.BCTL_DELINQUENCYSTATUS
  columns:
    - name: DELINQUENCYSTATUSID
      datatype: VARCHAR
    - name: DELINQUENCYSTATUSNAME
      datatype: VARCHAR