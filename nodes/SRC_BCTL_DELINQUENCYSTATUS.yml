name: SRC_BCTL_DELINQUENCYSTATUS
type: source
source:
  database: GUIDEWIRE
  schema: BCTL
  table: BCTL_DELINQUENCYSTATUS
  location: SRC (GUIDEWIRE.BCTL.BCTL_DELINQUENCYSTATUS)
columns:
  - name: ID
    data_type: INTEGER
    nullable: true
  - name: NAME
    data_type: STRING
    nullable: true