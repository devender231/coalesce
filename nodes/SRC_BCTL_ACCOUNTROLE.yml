name: SRC_BCTL_ACCOUNTROLE
type: source
location:
  name: SRC_BCTL_ACCOUNTROLE
  database: "{{DATABASE}}"
  schema: "{{SCHEMA}}"
columns:
  - name: ID
    data_type: INTEGER
    nullable: true
  - name: TYPECODE
    data_type: STRING
    nullable: true