name: SRC_DIMBILLINGACCOUNT_ROWCOUNT_UPDATE
type: row_count
description: Counts update rows (BeanVersion different)
variable: User::UpdateCount
input_dependency: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
input_path: update_rows
execution_order: 6

transformation_logic:
  operation: count_rows
  counter_variable: User::UpdateCount
  input_source:
    component: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
    output_path: update_rows
  
metadata:
  source_system: SSIS
  transformation_type: ROW_COUNT
  purpose: Track count of update records where BeanVersion is different
  parallel_execution: true
  
validation:
  required_inputs:
    - SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
  output_variable: User::UpdateCount
  variable_type: integer