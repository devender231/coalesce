name: SRC_TR_ROWCOUNT_SOURCE
type: transformation
config:
  transformation_type: row_count
  description: "Counts source rows from OLE_SRC - GuideWire"
  input_source: SRC_JOIN_ALL_TABLES
  logic:
    operation: count_rows
    variable_name: "User::SourceCount"
    count_type: total_rows
  output:
    pass_through: true
    add_count_column: false
  execution_order: 2
  dependencies:
    - SRC_JOIN_ALL_TABLES