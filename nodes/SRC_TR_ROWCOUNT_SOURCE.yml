name: SRC_TR_ROWCOUNT_SOURCE
type: transformation
transformation_type: ROW_COUNT
config:
  description: "Counts source rows from OLE_SRC - GuideWire"
  input_source: "SRC_BC_ACCOUNT"
  logic:
    count_variable: "User::SourceCount"
    count_type: "ALL_ROWS"
  output:
    pass_through: true
    add_count_column: false
  execution_order: 1
  
dependencies:
  - "SRC_BC_ACCOUNT"

metadata:
  purpose: "Audit and tracking of source row counts"
  variable_scope: "Package"
  variable_type: "Integer"
  ssis_component: "Row Count Transformation"