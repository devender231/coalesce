name: SRC_TR_ROWCOUNT_SOURCE
type: transformation
transformation_type: ROW_COUNT
execution_sequence: 1
description: Counts source rows from OLE_SRC - GuideWire

input:
  source: SRC_JOIN_DIMBILLINGACCOUNT
  description: Takes joined dataset as input

logic:
  operation: COUNT_ROWS
  variable: User::SourceCount
  description: Stores total row count in variable User::SourceCount

output:
  name: ROWCOUNT_SOURCE_OUTPUT
  columns: ALL
  metadata:
    row_count_variable: User::SourceCount