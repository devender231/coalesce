name: SRC_TR_ROWCOUNT_SOURCE
type: transformation
transformation_type: ROW_COUNT
description: Counts source rows from OLE_SRC - GuideWire
execution_order: 1
dependencies:
  - SRC_JOIN_DIMBILLINGACCOUNT

logic:
  operation: COUNT_ROWS
  variable_name: User::SourceCount
  description: Captures total number of rows from source before any transformations

input:
  source: SRC_JOIN_DIMBILLINGACCOUNT
  
output:
  row_count_variable: User::SourceCount
  pass_through: true
  
metadata:
  package_name: EDW_BC_Load_DimBillingAccount
  component_name: Row Count - Source
  purpose: Audit and tracking of source row counts