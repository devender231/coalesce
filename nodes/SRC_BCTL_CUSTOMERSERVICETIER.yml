name: SRC_BCTL_CUSTOMERSERVICETIER
type: source
config:
  source_location: "GUIDEWIRE.BCTL"
  columns:
    - name: ID
      data_type: INTEGER
      nullable: true
    - name: NAME
      data_type: STRING
      nullable: true