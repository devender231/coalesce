# SSIS Package: EDW_BC_Load_DimBillingAccount
# Component: Transformation 5 - ROW_COUNT (Unchanged Count)
# Generated from SSIS documentation

name: SRC_ROW_COUNT_UNCHANGED
type: transformation
transformation_type: ROW_COUNT
description: Counts unchanged rows (BeanVersion matches)

transformation_details:
  execution_order: 5
  component_name: ROW_COUNT
  operation: Count unchanged rows
  source_output: Conditional Split - Unchanged_Records
  variable_name: User::UnChangeCount
  variable_type: Integer
  
logic:
  description: Counts rows where BeanVersion matches EDWBeanVersion (no changes needed)
  condition: BeanVersion == EDWBeanVersion
  variable_assignment: User::UnChangeCount = COUNT(*)
  
input:
  source_type: Conditional Split Output
  source_name: Unchanged_Records
  filter_condition: BeanVersion == EDWBeanVersion
  
output:
  variable: User::UnChangeCount
  description: Total count of unchanged rows (no updates needed)
  action: No further processing (records already current)
  
metadata:
  ssis_package: EDW_BC_Load_DimBillingAccount
  component_type: Transformation
  transformation_sequence: 5
  generated_from: SSIS documentation