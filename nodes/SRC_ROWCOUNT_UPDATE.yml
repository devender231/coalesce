name: SRC_ROWCOUNT_UPDATE
type: transformation
transformation_type: row_count
config:
  description: "Counts update rows (BeanVersion different)"
  execution_order: 7
  variable_name: "User::UpdateCount"
source:
  - SRC_CONDITIONALSPLIT_BEANVERSION
  source_path: update
logic:
  count_type: all_rows
  output_variable: "User::UpdateCount"
  description: "Counts rows where BeanVersion differs from EDWBeanVersion - existing records to update"
output:
  pass_through: true
  add_count_column: false
  route_to_target: true