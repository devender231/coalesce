name: SRC_BCTL_BILLINGLEVEL
type: source

source:
  location: SRC ("GUIDEWIRE"."BCTL")
  
columns:
  - name: ID
    data_type: INTEGER
    nullable: true
  - name: NAME
    data_type: STRING
    nullable: true