name: SRC_BCTL_BILLINGLEVEL
type: source
config:
  source_location: SRC (DATABASE.SCHEMA)
  source_table: BCTL_BILLINGLEVEL
  columns:
    - name: ID
      data_type: INTEGER
      nullable: true
    - name: NAME
      data_type: STRING
      nullable: true