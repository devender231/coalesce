name: SRC_DIMBILLINGACCOUNT_ROWCOUNT_UNCHANGED
type: transformation
transformation_type: ROW_COUNT
description: Counts unchanged rows (BeanVersion matches)
source:
  - SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT
  - output: UNCHANGED_ROWS
logic:
  operation: COUNT
  output_variable: User::UnChangeCount
  description: Counts rows where BeanVersion matches EDWBeanVersion
output:
  row_count_variable: UnChangeCount
  pass_through: false
  termination: true