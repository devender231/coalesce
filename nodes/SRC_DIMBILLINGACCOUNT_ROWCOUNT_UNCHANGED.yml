name: SRC_DIMBILLINGACCOUNT_ROWCOUNT_UNCHANGED
type: row_count
description: Counts unchanged rows (BeanVersion matches)
variable: User::UnChangeCount
input_dependency: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
input_path: unchanged_rows
execution_order: 6

transformation_logic:
  operation: count_rows
  counter_variable: User::UnChangeCount
  input_source:
    component: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
    output_path: unchanged_rows
  
metadata:
  source_system: SSIS
  transformation_type: ROW_COUNT
  purpose: Track count of unchanged records where BeanVersion matches
  parallel_execution: true
  
validation:
  required_inputs:
    - SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
  output_variable: User::UnChangeCount
  variable_type: integer