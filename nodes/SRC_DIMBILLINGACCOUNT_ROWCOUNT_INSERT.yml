name: SRC_DIMBILLINGACCOUNT_ROWCOUNT_INSERT
type: row_count
description: Counts new insert rows (lookup no match)
variable: User::InsertCount
input_dependency: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
input_path: insert_rows
execution_order: 6

transformation_logic:
  operation: count_rows
  counter_variable: User::InsertCount
  input_source:
    component: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
    output_path: insert_rows
  
metadata:
  source_system: SSIS
  transformation_type: ROW_COUNT
  purpose: Track count of new insert records where lookup had no match
  parallel_execution: true
  
validation:
  required_inputs:
    - SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT_BEANVERSION
  output_variable: User::InsertCount
  variable_type: integer