fileVersion: 1.0
name: SRC_TRANSFORM_ROWCOUNT_UPDATE
description: Counts update rows (BeanVersion different)
type: RowCount
config:
  transformation_type: ROW_COUNT
  logic:
    description: Counts update rows where BeanVersion differs from EDWBeanVersion
    variable: User::UpdateCount
    operation: COUNT_ROWS
  input:
    source: SRC_TRANSFORM_CONDITIONAL_SPLIT
    output_name: UpdateOutput
    description: Rows from conditional split where BeanVersion differs
  output:
    variable_name: UpdateCount
    variable_type: Integer
    scope: User

execution_order: 8
package: EDW_BC_Load_DimBillingAccount
dependencies:
  - SRC_TRANSFORM_CONDITIONAL_SPLIT