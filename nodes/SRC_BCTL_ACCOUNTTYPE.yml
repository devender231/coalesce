name: SRC_BCTL_ACCOUNTTYPE
type: source
config:
  source_name: BCTL_ACCOUNTTYPE
  source_type: table
  database: ""
  schema: ""
  location: SRC ("."."")
columns:
  - name: ACCOUNTTYPEKEY
    datatype: NUMBER(38,0)
    description: Primary key for account type
  - name: ACCOUNTTYPECODE
    datatype: VARCHAR2(50)
    description: Account type code
  - name: ACCOUNTTYPENAME
    datatype: VARCHAR2(200)
    description: Account type name
  - name: ACCOUNTTYPEDESCRIPTION
    datatype: VARCHAR2(500)
    description: Account type description