name: SRC_BCTL_ACCOUNTTYPE
type: source
location:
  database: BILLINGCENTER
  schema: BCTL
  table: BCTL_ACCOUNTTYPE
columns:
  - name: ID
    dataType: BIGINT
    nullable: true
  - name: NAME
    dataType: VARCHAR
    nullable: true