source:
  name: SRC_BCTL_ACCOUNTTYPE
  location: SRC("BC"."BCTL_ACCOUNTTYPE")
  columns:
    - name: ID
      data_type: INTEGER
      nullable: true
    - name: NAME
      data_type: STRING
      nullable: true
