name: SRC_BCTL_ACCOUNTTYPE
type: source
source:
  database: GUIDEWIRE
  schema: BCTL
  table: BCTL_ACCOUNTTYPE
  location: SRC (GUIDEWIRE.BCTL.BCTL_ACCOUNTTYPE)
columns:
  - name: ID
    data_type: INTEGER
    nullable: true
  - name: NAME
    data_type: STRING
    nullable: true