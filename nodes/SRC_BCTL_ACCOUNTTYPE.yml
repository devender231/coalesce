name: SRC_BCTL_ACCOUNTTYPE
type: source
config:
  source_location: SRC ("database"."schema")
  columns:
    - name: ID
      data_type: INTEGER
      nullable: true
    - name: NAME
      data_type: STRING
      nullable: true