name: SRC_BCTL_ACCOUNTTYPE
type: source
config:
  source_location: SRC (GUIDEWIRE.BCTL)
  table_name: BCTL_ACCOUNTTYPE
  columns:
    - name: ID
      data_type: INTEGER
      nullable: true
    - name: NAME
      data_type: STRING
      nullable: true