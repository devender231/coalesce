name: SRC_BCTL_STATE

nodeType: Source

locationName: GUIDEWIRE

database: GUIDEWIRE
schema: dbo
table: BCTL_STATE

config:
  isMultisource: false

sourceMapping:
  - name: SRC_BCTL_STATE
    database: GUIDEWIRE
    schema: dbo
    table: BCTL_STATE

    columns:
      - name: ID
        dataType: INTEGER
        nullable: true
      - name: NAME
        dataType: STRING
        nullable: true