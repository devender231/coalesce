name: SRC_BCTL_STATE
type: source
config:
  source_name: BCTL_STATE
  database: GUIDEWIRE
  schema: BCTL
  table: BCTL_STATE
  location: GUIDEWIRE.BCTL.BCTL_STATE
columns:
  - name: ID
    data_type: INTEGER
    nullable: true
  - name: NAME
    data_type: STRING
    nullable: true