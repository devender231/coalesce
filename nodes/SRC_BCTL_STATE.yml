name: SRC_BCTL_STATE
type: source
location:
  name: SRC_BCTL_STATE
  database: "{{DATABASE}}"
  schema: "{{SCHEMA}}"
columns:
  - name: ID
    data_type: INTEGER
    nullable: true
  - name: NAME
    data_type: STRING
    nullable: true