source:
  name: SRC_BCTL_STATE
  location: SRC("BC"."BCTL_STATE")
  columns:
    - name: ID
      data_type: INTEGER
      nullable: true
    - name: NAME
      data_type: STRING
      nullable: true
