fileVersion: 1
id: "SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT"
name: "SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT"
operation:
  config:
    insertStrategy: INSERT
    selectDistinct: false
  database: ""
  deployEnabled: true
  description: "Conditional Split transformation - Routes rows based on BeanVersion match (Unchanged, Insert, Update)"
  isMultisource: false
  locationName: "WORK"
  materializationType: "view"
  metadata:
    appliedNodeTests: []
    columns:
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: 1
          stepCounter: "SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT"
        config: {}
        dataType: "VARCHAR(16777216)"
        description: "All columns from lookup transformation"
        name: "*"
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: null
                stepCounter: "SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION"
            transform: "*"
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: 2
          stepCounter: "SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT"
        config: {}
        dataType: "VARCHAR(50)"
        description: "Output path indicator: UNCHANGED, INSERT, or UPDATE"
        name: "OUTPUT_PATH"
        nullable: false
        sourceColumnReferences:
          - columnReferences: []
            transform: |
              CASE 
                WHEN LOOKUP_BEANVERSION IS NULL THEN 'INSERT'
                WHEN BEANVERSION = LOOKUP_BEANVERSION THEN 'UNCHANGED'
                WHEN BEANVERSION != LOOKUP_BEANVERSION THEN 'UPDATE'
                ELSE 'DEFAULT'
              END
    cteString: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION: "SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION"
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: "WORK"
            nodeName: "SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION"
        join:
          joinCondition: |
            FROM {{ ref('WORK', 'SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION') }} SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION
        name: "SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT"
        noLinkRefs: []
  name: "SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT"
  overrideSQL: false
  schema: ""
  sqlType: "DT113:::156"
  transformationType: "conditionalsplit"
  type: "sql"
  conditionalSplitConfig:
    conditions:
      - name: "UNCHANGED_OUTPUT"
        condition: "BEANVERSION = LOOKUP_BEANVERSION"
        order: 1
      - name: "INSERT_OUTPUT"
        condition: "LOOKUP_BEANVERSION IS NULL"
        order: 2
      - name: "UPDATE_OUTPUT"
        condition: "BEANVERSION != LOOKUP_BEANVERSION"
        order: 3
    defaultOutput: "DEFAULT_OUTPUT"
  version: 1