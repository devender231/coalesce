fileVersion: 1
id: dimbillingaccount_conditionalsplit_node
name: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT
operation:
  config:
    clusterKey: false
    downstreamOption: true
    groupByAll: false
    initialize: ON_CREATE
    insertStrategy: INSERT
  database: ""
  deployEnabled: true
  description: Conditional Split transformation - Routes rows based on whether BeanVersion matches EDWBeanVersion
  isMultisource: false
  locationName: SRC
  materializationType: view
  metadata:
    appliedNodeTests: []
    columns:
      - columnReference:
          columnCounter: 21
          stepCounter: 1
        config: {}
        dataType: VARCHAR(50)
        description: Row routing indicator
        name: ROW_TYPE
        nullable: false
        sourceColumnReferences:
          - columnReferences: []
            transform: |-
              CASE 
                WHEN "EDW_BEANVERSION" IS NULL THEN 'INSERT'
                WHEN "BEANVERSION" = "EDW_BEANVERSION" THEN 'UNCHANGED'
                ELSE 'UPDATE'
              END
    cteString: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - dependencies:
          - locationName: SRC
            nodeName: SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION
        join:
          joinCondition: FROM {{ ref('SRC', 'SRC_DIMBILLINGACCOUNT_LOOKUP_BEANVERSION') }}
        name: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT
        noLinkRefs: []
  name: SRC_DIMBILLINGACCOUNT_CONDITIONALSPLIT
  overrideSQL: false
  schema: ""
  sqlType: Transform
  type: sql
  version: 1