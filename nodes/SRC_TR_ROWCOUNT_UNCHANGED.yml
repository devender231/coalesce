name: SRC_TR_ROWCOUNT_UNCHANGED
type: transformation
transformation_type: ROW_COUNT
description: Counts unchanged rows (BeanVersion matches)
execution_order: 5
dependencies:
  - SRC_TR_CONDITIONALSPLIT_SCD

logic:
  operation: COUNT_ROWS
  variable_name: User::UnChangeCount
  description: Captures count of rows where BeanVersion matches EDW (no updates needed)
  input_path: UNCHANGED_ROWS

input:
  source: SRC_TR_CONDITIONALSPLIT_SCD
  output_path: unchanged_rows
  
output:
  row_count_variable: User::UnChangeCount
  pass_through: false
  description: These rows are counted but not passed to target (no changes required)
  
metadata:
  package_name: EDW_BC_Load_DimBillingAccount
  component_name: Row Count - Unchanged
  purpose: Audit tracking of unchanged rows for ETL monitoring