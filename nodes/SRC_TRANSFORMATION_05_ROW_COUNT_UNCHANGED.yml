---
name: SRC_TRANSFORMATION_05_ROW_COUNT_UNCHANGED
type: transformation
transformation_type: ROW_COUNT
execution_order: 5
description: Counts unchanged rows where BeanVersion matches

source:
  type: upstream_transformation
  name: SRC_TRANSFORMATION_04_CONDITIONAL_SPLIT
  output_branch: UNCHANGED_ROWS
  description: Rows where BeanVersion matches EDWBeanVersion

transformation_logic:
  operation: COUNT_ROWS
  count_variable: User::UnChangeCount
  filter_condition: BeanVersion == EDWBeanVersion
  description: Count rows that require no updates

output:
  variable_name: User::UnChangeCount
  variable_type: Integer
  description: Total count of unchanged rows
  row_disposition: NO_ACTION_REQUIRED

upstream_dependencies:
  - SRC_TRANSFORMATION_04_CONDITIONAL_SPLIT

metadata:
  package_name: EDW_BC_Load_DimBillingAccount
  component_type: Row Count
  execution_sequence: 5
  purpose: Track count of rows with no changes for monitoring